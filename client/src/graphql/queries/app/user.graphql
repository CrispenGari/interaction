query AppUser {
  user {
    user {
      username
      gender
      uid
      chats {
        chatId
        updatedAt
        users {
          username
          uid
        }
      }
    }
  }
}
